if [[ "$OSTYPE" != "darwin"* ]]; then
    alias ls='ls -G'
else
    alias ls='ls --color=auto'
fi
alias lA='ls -a'
alias la='ls -A'
alias la='ls -Alh'
alias ll='ls -lh'
alias llA='ll -a'
alias lla='ll -A'

# vim: set ft=sh:
